"""One authoritative TFDS split implementation for both methods."""
from polygarbor.dataset import (  # noqa: F401
    DEFAULT_DATASET, DEFAULT_SPLITS, SPLIT_NAMES, DatasetBundle,
    export_samples, iter_numpy, load,
)
